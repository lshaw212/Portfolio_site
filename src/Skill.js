import React from 'react';
import Fade from 'react-reveal/Fade';

const Skill = ({time}) => (
  <Fade delay={time}>
    <div className="skill">
      Skill
    </div>
  </Fade>
)

export default Skill;